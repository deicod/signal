version: 2

run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - govet
    - staticcheck
    - revive

linters-settings:
  revive:
    rules:
      - name: package-comments
      - name: exported
      - name: blank-imports
