version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - revive
  settings:
    revive:
      rules:
        - name: package-comments
        - name: exported
        - name: blank-imports
